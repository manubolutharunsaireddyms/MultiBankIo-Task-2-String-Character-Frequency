# MultiBankIo-Task-2-String-Character-Frequency

A simple Java program to count the frequency of characters in a string using HashMap.
This program handles all types of characters including uppercase, lowercase, digits, spaces, and special symbols
Counts frequency of every character in the input string.
Supports all characters (letters, digits, special symbols, spaces).
Prints the frequency of each character in a clear format.
